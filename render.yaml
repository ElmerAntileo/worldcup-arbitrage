services:
  - type: web
    name: worldcup-arbitrage
    runtime: python3
    buildCommand: pip install --user -r requirements.txt
    startCommand: python -m uvicorn app:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
